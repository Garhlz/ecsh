use ./std_test.ecs as std

pub func demo(xs) {
    let obj = {name: "ecsh", nums: [1, 2, 3]}
    let out = text(cmd{ printf "ok"})
    return xs |> map((x) => x + 1) |> join(",")
}

println(std.sum([1, 2, 3]))

let a = [1,2,3]
a = [1, 2, 3] |> take(2)


